package Levels;
// import Projectiles.*;
// import Rendering.*;

/*
 * This is a sample level that just has a bunch of random projectiles.
 */
public class SampleLevelProjectile extends Level {
	// private ArrayList<BoardObject> projectiles;

	// public SampleLevelProjectile() {
	// 	super();
	// 	projectiles = new ArrayList<BoardObject>();
	// 	for (int i = 0; i < 100; i++) {
	// 		double rX = (Math.random() * 1000);
	// 		double rY = (Math.random() * 1000);
	// 		double rVel = (Math.random() * 10);
	// 		double rAng = (Math.random() * 365);
	// 		double rRad = (Math.random() * 20);
	// 		Color rCol = new Color((int) (Math.random() * 256), (int) (Math.random() * 256),
	// 				(int) (Math.random() * 256));
	// 		projectiles.add(new Projectile(rX, rY, rVel, rAng, rRad, rCol));
	// 	}
	// 	setBackgroundColor(StdDraw.WHITE);
	// 	for (BoardObject p : projectiles) {
	// 		addToLevel(p);
	// 	}
	// }
}
